#include<stdio.h>
int main()
{
	int a,b;
	printf("Enter the numbers ");
	scanf("%d %d",&a,&b);
	if(a==b)
	printf("Both the numbers are equal ",a,b);
	else
	printf("Both the numbers are not equal ",a,b);
	return 0;
}
