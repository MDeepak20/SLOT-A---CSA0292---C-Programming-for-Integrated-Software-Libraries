#include<stdio.h>
int main()
{
	int n,i,sumeven=0,sumodd=0;
	printf("Enter the number ");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
{
	if(i%2==0)
	sumeven+=i;
	else
	sumodd+=i;
}
	printf("%d is sum of even numbers",sumeven);
	printf("%d is sum of odd numbers",sumodd);
	return 0;
}
